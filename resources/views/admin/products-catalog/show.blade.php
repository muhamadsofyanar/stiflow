<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="font-semibold text-xl">{{ $product->name }}</h2><div class="flex gap-2"><a href="{{ route('admin.variants.create', ['product_id' => $product->id]) }}" class="px-4 py-2 bg-blue-600 text-white rounded-lg">Tambah Varian</a><a href="{{ route('admin.products-catalog.edit', $product) }}" class="px-4 py-2 bg-amber-500 text-white rounded-lg">Edit</a></div></div></x-slot><div class="py-10"><div class="max-w-5xl mx-auto space-y-6"><div class="bg-white dark:bg-gray-800 rounded-2xl p-6 grid md:grid-cols-3 gap-5"><div><div class="text-xs text-gray-500">Tipe</div><div>{{ $product->type?->value ?? $product->type }}</div></div><div><div class="text-xs text-gray-500">Harga</div><div>Rp{{ number_format((float) $product->price, 0, ',', '.') }}</div></div><div><div class="text-xs text-gray-500">Status</div><div>{{ $product->status?->value ?? $product->status }}</div></div><div class="md:col-span-3 whitespace-pre-line">{{ $product->description ?: 'Tanpa deskripsi' }}</div></div><div class="bg-white dark:bg-gray-800 rounded-2xl p-6"><h3 class="font-bold mb-4">Varian ({{ $product->variants->count() }})</h3>@forelse($product->variants as $variant)<div class="border-t py-3 flex justify-between"><a href="{{ route('admin.variants.show', $variant) }}" class="font-semibold text-blue-600">{{ $variant->name }} · {{ $variant->sku }}</a><span>{{ $variant->price_override ? 'Rp'.number_format((float) $variant->price_override, 0, ',', '.') : 'Harga utama' }}</span></div>@empty<p class="text-gray-500">Belum ada varian.</p>@endforelse</div></div></div></x-app-layout>
