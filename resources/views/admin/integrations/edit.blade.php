<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="font-semibold text-xl">Edit Integrasi: {{ $integration->display_name }}</h2><form method="POST" action="{{ route('admin.integrations.destroy', $integration) }}" onsubmit="return confirm('Hapus integrasi ini?')">@csrf @method('DELETE')<button class="px-4 py-2 bg-red-600 text-white rounded-lg">Hapus</button></form></div></x-slot><div class="py-10"><form method="POST" action="{{ route('admin.integrations.update', $integration) }}" class="max-w-4xl mx-auto bg-white dark:bg-gray-800 rounded-2xl p-8 space-y-6">@csrf @method('PUT') @include('admin.integrations._form')<div class="text-right"><button class="px-6 py-2 bg-blue-600 text-white rounded-lg">Simpan</button></div></form></div></x-app-layout>
