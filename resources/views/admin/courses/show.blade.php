<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="font-semibold text-xl">{{ $course->title }}</h2><a href="{{ route('admin.courses.edit', $course) }}" class="px-4 py-2 bg-amber-500 text-white rounded-lg">Edit</a></div></x-slot><div class="py-10"><div class="max-w-5xl mx-auto space-y-6"><div class="bg-white dark:bg-gray-800 rounded-2xl p-6"><p class="text-gray-500">{{ $course->summary ?: 'Tanpa ringkasan' }}</p><div class="mt-4 whitespace-pre-line">{{ $course->description }}</div></div>@forelse($course->modules as $module)<div class="bg-white dark:bg-gray-800 rounded-2xl p-6"><h3 class="font-bold">{{ $module->position + 1 }}. {{ $module->title }}</h3><ul class="mt-3 space-y-2">@foreach($module->lessons as $lesson)<li class="border-t py-2">{{ $lesson->position + 1 }}. {{ $lesson->title }} <span class="text-xs text-gray-500">({{ $lesson->lesson_type?->value ?? $lesson->lesson_type }})</span></li>@endforeach</ul></div>@empty<div class="bg-white dark:bg-gray-800 rounded-2xl p-10 text-center text-gray-500">Belum ada modul.</div>@endforelse</div></div></x-app-layout>
