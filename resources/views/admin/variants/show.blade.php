<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="font-semibold text-xl">{{ $variant->name }}</h2><a href="{{ route('admin.variants.edit', $variant) }}" class="px-4 py-2 bg-amber-500 text-white rounded-lg">Edit</a></div></x-slot><div class="py-10"><div class="max-w-4xl mx-auto px-4"><div class="bg-white dark:bg-gray-800 rounded-2xl p-6 grid md:grid-cols-2 gap-5"><div><div class="text-gray-500 text-sm">Produk</div><div class="font-semibold">{{ $variant->product?->name }}</div></div><div><div class="text-gray-500 text-sm">SKU</div><div class="font-semibold">{{ $variant->sku }}</div></div><div><div class="text-gray-500 text-sm">Harga</div><div>{{ $variant->price_override ? 'Rp'.number_format((float)$variant->price_override, 0, ',', '.') : 'Mengikuti harga produk' }}</div></div><div><div class="text-gray-500 text-sm">Stok</div><div>{{ $variant->stock_qty ?? 'Tidak dibatasi' }}</div></div></div></div></div></x-app-layout>
