<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="font-semibold text-xl">Varian Produk</h2><a href="{{ route('admin.variants.create') }}" class="px-4 py-2 bg-blue-600 text-white rounded-lg">+ Tambah Varian</a></div></x-slot><div class="py-10"><div class="max-w-6xl mx-auto px-4 space-y-4">@if(session('status'))<div class="p-4 bg-green-50 text-green-700 rounded-lg">{{ session('status') }}</div>@endif<div class="bg-white dark:bg-gray-800 rounded-2xl overflow-hidden"><table class="w-full text-sm"><thead><tr class="bg-gray-50 dark:bg-gray-700"><th class="p-4 text-left">Varian</th><th class="p-4 text-left">Produk</th><th class="p-4 text-left">SKU</th><th class="p-4 text-right">Harga</th><th class="p-4"></th></tr></thead><tbody>@forelse($variants as $variant)<tr class="border-t"><td class="p-4 font-semibold">{{ $variant->name }}</td><td class="p-4">{{ $variant->product?->name }}</td><td class="p-4">{{ $variant->sku }}</td><td class="p-4 text-right">{{ $variant->price_override ? 'Rp'.number_format((float)$variant->price_override, 0, ',', '.') : 'Harga utama' }}</td><td class="p-4 text-right"><a href="{{ route('admin.variants.show', $variant) }}" class="text-blue-600">Lihat</a></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-gray-500">Belum ada varian.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $variants->links() }}</div></div></div></div></x-app-layout>
