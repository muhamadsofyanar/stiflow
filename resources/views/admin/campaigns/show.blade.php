<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="font-semibold text-xl">{{ $campaign->name }}</h2><a href="{{ route('admin.campaigns.edit', $campaign) }}" class="px-4 py-2 bg-amber-500 text-white rounded-lg">Edit</a></div></x-slot><div class="py-10"><div class="max-w-5xl mx-auto space-y-6"><div class="bg-white dark:bg-gray-800 rounded-2xl p-6 grid md:grid-cols-3 gap-5"><div><div class="text-xs text-gray-500">Channel</div>{{ $campaign->channel?->value ?? $campaign->channel }}</div><div><div class="text-xs text-gray-500">Status</div>{{ $campaign->status?->value ?? $campaign->status }}</div><div><div class="text-xs text-gray-500">Jadwal</div>{{ $campaign->schedule_send_at?->format('d-m-Y H:i') ?? '-' }}</div><div><div class="text-xs text-gray-500">Template</div>{{ $campaign->messageTemplate?->name ?? '-' }}</div><div><div class="text-xs text-gray-500">Audiens</div>{{ $campaign->contactList?->name ?? $campaign->segment?->name ?? '-' }}</div><div><div class="text-xs text-gray-500">Pengirim</div>{{ $campaign->senderIntegrationConnection?->display_name ?? '-' }}</div></div><div class="bg-white dark:bg-gray-800 rounded-2xl p-6"><h3 class="font-bold mb-4">Penerima ({{ $campaign->recipients->count() }})</h3>@forelse($campaign->recipients as $recipient)<div class="border-t py-3 flex justify-between"><span>{{ $recipient->destination }}</span><span>{{ $recipient->status?->value ?? $recipient->status }}</span></div>@empty<p class="text-gray-500">Belum ada penerima.</p>@endforelse</div></div></div></x-app-layout>
