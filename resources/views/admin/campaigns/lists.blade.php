<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">Daftar Kontak Campaign</h2></x-slot><div class="py-10"><div class="max-w-6xl mx-auto px-4 grid lg:grid-cols-3 gap-6"><form method="POST" action="{{ route('admin.lists.store') }}" class="bg-white dark:bg-gray-800 rounded-2xl p-6 space-y-4">@csrf<h3 class="font-bold">Daftar Baru</h3><div><x-input-label for="name" value="Nama *"/><x-text-input id="name" name="name" required class="mt-1 w-full"/></div><div><x-input-label for="description" value="Deskripsi"/><textarea id="description" name="description" class="mt-1 w-full rounded-lg border-gray-300 dark:bg-gray-800"></textarea></div><label class="flex gap-2"><input type="checkbox" name="is_marketable_only" value="1" checked>Hanya kontak marketing</label><x-primary-button>Simpan</x-primary-button></form><div class="lg:col-span-2 bg-white dark:bg-gray-800 rounded-2xl p-6">@forelse($lists as $list)<div class="py-4 border-b flex justify-between"><div><div class="font-semibold">{{ $list->name }}</div><div class="text-sm text-gray-500">{{ $list->members_count }} kontak · {{ $list->description }}</div></div><form method="POST" action="{{ route('admin.lists.destroy', $list) }}">@csrf @method('DELETE')<button class="text-red-600">Hapus</button></form></div>@empty<p class="text-gray-500">Belum ada daftar kontak.</p>@endforelse<div class="mt-4">{{ $lists->links() }}</div></div></div></div></x-app-layout>
